#!/bin/sh

/usr/local/bin/tarsnap \
    --keyfile /etc/tarsnap/tarsnap.key \
    --cachedir /data/tarsnap.cache/ \
    -c -f "nelhage.com-$(date +%Y%m%d-%H%M)" \
    /data/archived /data/backup /opt /data/home 2>&1 | /opt/go/bin/unilog /data/log/tarsnap.log

